<nav>
    <a href="/">5</a>
    <!-- <a href="/wtf">WTF?</a> -->
    <a href="/results">RESULTS</a>
</nav>
<style>
	nav {
		z-index: 10;
		position: sticky;
		top: 0;
		display: flex;
		font-family: "blackout_midnight", "Impact", Arial;
		justify-content: end;
		padding: 0 2rem;
		gap: 0.25rem;
		color: var(--text);
		max-width: var(--max-width);
	}
	nav a {
		padding: 0.25rem 1rem;
		background: var(--text);
		color: var(--background-color);
		text-decoration: none;
	}
	nav a:hover {
		color: var(--text);
		background: var(--background-color-2);
	}
</style>
